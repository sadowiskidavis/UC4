// Crio a interface Usuario
// qualquer objeto ou classe que implementa/use esta interface PRECISA seguiras regras dela, ou seja ter todos os seus atributos, tipos e metodos (se houver)
export interface IUsuario {
    nome: string
    idade:number
}
